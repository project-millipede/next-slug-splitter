# Dashboard

This is another **heavy** page — it uses multiple custom React components.

## Revenue Chart

<Chart />

## Recent Transactions

<DataTable />

This page imports both `<Chart />` and `<DataTable />`, making it the
heaviest page in the demo. Its generated handler bundles exactly these
two components — nothing more, nothing less.
