# Dashboard

Dies ist ebenfalls eine **schwere** Seite — sie nutzt mehrere eigene
React-Komponenten.

## Umsatzdiagramm

<Chart />

## Letzte Transaktionen

<DataTable />

Diese Seite importiert sowohl `<Chart />` als auch `<DataTable />` und ist
damit die schwerste Seite der Demo. Ihr generierter Handler bündelt genau diese
zwei Komponenten — nicht mehr und nicht weniger.
