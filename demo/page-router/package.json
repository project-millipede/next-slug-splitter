{
  "name": "next-slug-splitter-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist && rimraf .next",
    "generate:ballast": "node scripts/generate-ballast.mjs",
    "clean:handlers": "node scripts/clean-handlers.mjs",
    "clean:all": "node scripts/erase-generated-dev-state.mjs",
    "dev": "pnpm generate:ballast && pnpm clean:handlers && next dev",
    "dev:ts": "pnpm generate:ballast && pnpm clean:handlers && next dev",
    "dev:js-pkg": "pnpm generate:ballast && pnpm clean:handlers && next dev",
    "dev:ts-pkg": "pnpm generate:ballast && pnpm clean:handlers && next dev",
    "build": "pnpm generate:ballast && pnpm clean:handlers && next build",
    "build:ts": "pnpm generate:ballast && pnpm clean:handlers && next build",
    "build:js-pkg": "pnpm generate:ballast && pnpm clean:handlers && next build",
    "build:ts-pkg": "pnpm generate:ballast && pnpm clean:handlers && next build",
    "start": "next start",
    "start:ts": "next start",
    "start:js-pkg": "next start",
    "start:ts-pkg": "next start",
    "analyze:turbo": "pnpm next experimental-analyze"
  },
  "dependencies": {
    "@demo/components": "workspace:*",
    "@fal-works/esbuild-plugin-global-externals": "^2.1.2",
    "@mdx-js/esbuild": "^3.1.1",
    "esbuild": "^0.28.0",
    "next": "16.2.0",
    "next-slug-splitter": "file:../..",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "6.0.2"
  },
  "pnpm": {
    "patchedDependencies": {
      "next@16.2.0": "../../patches/next@16.2.0.patch"
    }
  }
}
